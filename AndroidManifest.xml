<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.delyan.fragmentbugs"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="4"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".Tests"
            android:theme="@style/Dialog">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
        <activity android:name="tests.NestedAnimations"></activity>
        <activity android:name="tests.NestedAnimationsNative"></activity>
        <activity android:name="tests.RetainInstances"></activity>
        <activity android:name="tests.RetainInstancesNative"></activity>
        <activity android:name="tests.ActivityResult"></activity>
        <activity android:name="tests.ActivityResultNative"></activity>
    </application>

</manifest>